#include "Monster.h"

/* constructors */
Monster::Monster(unsigned char symbol, int color, bool visible, int attitude, bool shopkeeper, bool priest, bool minotaur) : symbol(symbol), color(color), visible(visible), attitude(attitude), shopkeeper(shopkeeper), priest(priest), minotaur(minotaur) {
}
