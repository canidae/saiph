#include "ItemData.cpp"

ItemData::ItemData(const std::string& name, int base_cost, int weight, bool magic, char item_class) : name(name), base_cost(base_cost), weight(weight), magic(magic), item_class(item_class) {
}