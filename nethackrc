# configuration file for saiph.
#
# saiph is a bot playing nethack. the code is written in c++.
# for more information about this and other bots, see:
# http://sartak.org/nh/bots.html
#
# saiph is GPLv2 licenced.
#
# author: Vidar Wahlberg, canidae@exent.net

##########################################

# significant changes:
# * monsters
# - ghosts/shades are represented by 'X', not ' '
# - the ']' for mimic is now 'm', if that makes a difference
# * objects
# - illegal object is represented by 'm'
# - boulders are represented by '0', not '`'
# - iron balls are represented by '`', not '0'
# - chains are represented by '`', not '_'
# - venom is representated by '`', not '.'
# * dungeon
# - closed doors are represented by '7', not '+'
# - graves are represented by '\', not '|'
# - sinks are represented by '{', not '#'
# - raised drawbridges are represented by '8'. not '#'
# * traps
# - web is represented by '^', not '"'
OPTIONS=align:lawful
OPTIONS=autopickup
OPTIONS=boulder:0
OPTIONS=catname:Sebbe
OPTIONS=!cmdassist
OPTIONS=color
OPTIONS=disclose:-i -a -v -g -c
OPTIONS=dogname:Eidolos
OPTIONS=dungeon: |--------||.-|77##.##<><>_\\\\{{}.}..88 #}
OPTIONS=fruit:byte
OPTIONS=gender:random
OPTIONS=hilite_pet
OPTIONS=horsename:futilius
OPTIONS=!legacy
OPTIONS=lit_corridor
OPTIONS=!mail
OPTIONS=menustyle:partial
OPTIONS=monsters:abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ@X'&;:~m
OPTIONS=name:saiph
OPTIONS=!null
OPTIONS=number_pad:0
OPTIONS=objects:m)[="(%!?+/$*````
OPTIONS=pettype:none
OPTIONS=pickup_burden:Burdened
OPTIONS=pickup_types:$
OPTIONS=!prayconfirm
OPTIONS=race:human
OPTIONS=role:valkyrie
OPTIONS=runmode:teleport
OPTIONS=!sparkle
OPTIONS=!splash_screen
OPTIONS=time
OPTIONS=!timed_delay
OPTIONS=traps:\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^
